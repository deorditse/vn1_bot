networks:
  vn1:
    name: vn1

services:
  orchestrator-skill:
    build:
      context: ${REPO_ROOT:-.}
      dockerfile: skills/orchestrator-skill/Dockerfile
    expose:
      - "8020"
    env_file:
      - path: ${REPO_ROOT:-.}/skills/orchestrator-skill/.env
        required: false
    restart: unless-stopped
    networks:
      - vn1
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "1"
